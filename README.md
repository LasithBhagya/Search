# fSearch

or something (don't have a name yet)

> This program **must** be run as **an administrator** for the file indexer to work.

**Command:** <br>
`cmake -G "MinGW Makefiles" -B build && mingw32-make -C build -j4 && sudo start "C:/Users/lasit/Documents/Programs/C++/Search/build/Search.exe"`
